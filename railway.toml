[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate --noinput && gunicorn nxthelp.wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
